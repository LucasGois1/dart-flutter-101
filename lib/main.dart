import 'package:flutter/cupertino.dart';

import '1-child/main.dart';

void main() {
  runApp(const MyAppChild());
  // runApp(const MyAppProvider());
  // runApp(const MyAppChangeNotifier());

  // Streams

  // runApp(const MyAppMyBlock());
  // runApp(const MyAppPackBloC());
}
